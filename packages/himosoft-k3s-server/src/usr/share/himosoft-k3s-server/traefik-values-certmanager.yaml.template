# cert-manager HTTP-01 challenges use standard Ingress resources (not IngressRoute).
ingressClass:
  enabled: true
  isDefaultClass: true
  name: traefik

providers:
  kubernetesIngress:
    enabled: true
    ingressClass: traefik
